# .env.example — placeholder values ONLY (SEC-SECRETS-02).
# Copy to .env (gitignored) and fill in real values.
# Each variable: NAME=            # where to get the value
#
# todomini needs no credentials today; the pattern below shows the discipline for
# when it grows one (server-side name — this stack has no client bundle to leak into).
# TODOMINI_DB_URL=               # sqlite path or postgres URL, when the store gets real
