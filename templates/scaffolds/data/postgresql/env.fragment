# --- postgresql (merge into .env.example; values here are local-compose defaults) ---
DATABASE_URL=postgresql://app:localdev@localhost:5432/app  # prod: from SSM/Secrets Manager (SEC-SECRETS)
PGPOOL_MIN_SIZE=1                                          # STK-PG-03: pool sized deliberately
PGPOOL_MAX_SIZE=10
