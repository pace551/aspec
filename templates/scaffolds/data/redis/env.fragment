# --- redis/valkey (merge into .env.example) ---
REDIS_URL=redis://localhost:6379/0  # prod: ElastiCache endpoint + AUTH from SSM (SEC-SECRETS)
CACHE_TTL_SECONDS=300               # default TTL — every key gets one (STK-REDIS-02)
